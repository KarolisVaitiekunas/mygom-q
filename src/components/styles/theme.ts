const colors = {
  primary: '#0095ff',
  secondary: '#fff',
  background: '#323437',
};

const fontSizes = [12, 14, 16, 20, 24, 32];

const fonts = {
  size: {
    extraSmall: fontSizes[0],
    small: fontSizes[1],
    medium: fontSizes[2],
    large: fontSizes[3],
    extraLarge: fontSizes[4],
    huge: fontSizes[5],
  },
};

export const theme = {
  colors: colors,
  fontSizes: fontSizes,
  fonts: fonts,
};
